schema = 1
id = "skywars"
name = "SkyWars"
version = "dev"
type = "MINIGAME"
api = "^1.0"
entry = "src/main.lua"
authors = ["Blueva", "Whiron"]
description = "Spawn on floating islands, loot chests, and outlast rivals in the shrinking final storm."
disabledRequirements = ["SPAWNS"]

[replaces]
legacyId = "skywars"

[[setup.steps]]
id = "region"
required = true
displayName = "Configure Region"
description = "Configure the module-specific region setup data."
commandExamples = ["/baa game <arena> skywars region"]
expectedInput = "selection region"

[[setup.steps]]
id = "team"
required = true
displayName = "Configure Team"
description = "Configure the module-specific team setup data."
commandExamples = ["/baa game <arena> skywars team"]
expectedInput = "team count and team size"

[[setup.commands]]
name = "region"
usage = "/baa game <arena> skywars region"
description = "Configure region setup data."
requiredHint = true

[[setup.commands]]
name = "team"
usage = "/baa game <arena> skywars team"
description = "Configure team setup data."
requiredHint = true
