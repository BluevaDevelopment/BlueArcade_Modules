schema = 1
id = "snowball_fight"
name = "Snowball Fight"
version = "dev"
type = "MICROGAME"
api = "^1.0"
entry = "src/main.lua"
authors = ["Blueva", "Whiron"]
description = "Throw snowballs and outlast your opponents!"

[replaces]
legacyId = "snowball_fight"

[[setup.steps]]
id = "setmode"
required = true
displayName = "Configure Setmode"
description = "Configure the module-specific setmode setup data."
commandExamples = ["/baa game <arena> snowball_fight setmode"]
expectedInput = "mode"

[[setup.commands]]
name = "setmode"
usage = "/baa game <arena> snowball_fight setmode"
description = "Configure setmode setup data."
requiredHint = true
