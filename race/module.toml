schema = 1
id = "race"
name = "Race"
version = "1.0.0"
type = "MICROGAME"
api = "^1.0"
entry = "src/main.lua"
authors = ["Blueva", "Whiron"]
description = "First to the finish line wins!"

[replaces]
legacyId = "race"

[[setup.steps]]
id = "finishline"
required = true
displayName = "Configure Finishline"
description = "Configure the module-specific finishline setup data."
commandExamples = ["/baa game <arena> race finishline"]
expectedInput = "selection region"

[[setup.commands]]
name = "finishline"
usage = "/baa game <arena> race finishline"
description = "Configure finishline setup data."
requiredHint = true
